doctype html 
html(lang='en') 
  head 
    title= title
  body 
    h1 Add new store
    form(action="/stores/new", method="POST")
      .form-row
        label(for="storeName") Store Name: 
        input#productName(type="text", name="storeName")
      button(type="submit") Add new store